# 正式数据库：MySQL 8.0.16+（数据库由 database/after_sales.sql 一次性人工初始化）。
# 连接参数优先读取环境变量 APP_DB_DRIVER / APP_DB_URL / APP_DB_USER / APP_DB_PASSWORD，
# 未设置时使用以下默认值；默认密码为空会导致连接失败，运行前请务必设置 APP_DB_PASSWORD。
jdbc.driver=com.mysql.cj.jdbc.Driver
jdbc.url=jdbc:mysql://localhost:3306/after_sales?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
jdbc.username=after_sales_app
jdbc.password=
