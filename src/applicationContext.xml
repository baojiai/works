<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- Root application context. Existing services remain untouched until later migration stages. -->
    <context:component-scan base-package="com.course.aftersales.service,com.course.aftersales.repository"/>
    <context:property-placeholder location="classpath:jdbc.properties"
                                  system-properties-mode="OVERRIDE"
                                  ignore-unresolvable="false"/>

    <!-- 环境变量 APP_DB_* 优先，未设置时回退 jdbc.properties 默认值（MySQL）。 -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="#{systemEnvironment['APP_DB_DRIVER'] ?: '${jdbc.driver}'}"/>
        <property name="url" value="#{systemEnvironment['APP_DB_URL'] ?: '${jdbc.url}'}"/>
        <property name="username" value="#{systemEnvironment['APP_DB_USER'] ?: '${jdbc.username}'}"/>
        <property name="password" value="#{systemEnvironment['APP_DB_PASSWORD'] ?: '${jdbc.password}'}"/>
    </bean>

    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean" primary="true">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis-config.xml"/>
    </bean>

    <!-- The package is intentionally empty in stage 3 and will be populated during DAO migration. -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.course.aftersales.mapper"/>
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
    </bean>

    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    <tx:annotation-driven transaction-manager="transactionManager"/>
</beans>
