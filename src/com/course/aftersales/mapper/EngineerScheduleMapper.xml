<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.course.aftersales.mapper.EngineerScheduleMapper">
    <select id="findActiveSlots" resultType="map">SELECT * FROM standard_time_slot WHERE status='ACTIVE' ORDER BY start_time</select>
    <select id="findSchedules" resultType="map">SELECT es.*,st.name slot_name,st.start_time,st.end_time FROM engineer_schedule es JOIN standard_time_slot st ON st.slot_id=es.slot_id WHERE es.engineer_id=#{engineerId} AND es.service_date&gt;=CURRENT_DATE ORDER BY es.service_date,st.start_time</select>
    <select id="findEligibleEngineerId" resultType="long">SELECT ep.engineer_id FROM engineer_profile ep JOIN system_user u ON u.user_id=ep.engineer_id WHERE ep.engineer_id=#{engineerId} AND ep.qualification_status='APPROVED' AND ep.employment_status='ACTIVE' AND u.status='ACTIVE'</select>
    <insert id="insertSchedule">INSERT INTO engineer_schedule(engineer_id,service_date,slot_id,status) VALUES(#{engineerId},#{serviceDate},#{slotId},'AVAILABLE')</insert>
    <update id="closeSchedule">UPDATE engineer_schedule SET status='CLOSED',version_no=version_no+1 WHERE schedule_id=#{scheduleId} AND engineer_id=#{engineerId} AND status='AVAILABLE'</update>
    <insert id="insertOperationLog">INSERT INTO operation_log(user_id,operation_type,business_type,business_id,description) VALUES(#{userId},#{operation},#{businessType},#{businessId},#{description})</insert>
</mapper>
