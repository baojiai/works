<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.course.aftersales.mapper.NotificationMapper">

    <select id="findByUserId" resultType="map">
        SELECT *
        FROM notification
        WHERE receiver_id = #{userId}
        ORDER BY is_read, created_at DESC
    </select>

    <select id="countUnread" resultType="long">
        SELECT COUNT(*)
        FROM notification
        WHERE receiver_id = #{userId} AND is_read = FALSE
    </select>

    <update id="markRead">
        UPDATE notification
        SET is_read = TRUE
        WHERE notification_id = #{notificationId} AND receiver_id = #{userId}
    </update>

    <update id="markAllRead">
        UPDATE notification
        SET is_read = TRUE
        WHERE receiver_id = #{userId}
    </update>

</mapper>
